server {
    listen 443 ssl;
    server_name  _;
    underscores_in_headers on;

    root /usr/share/nginx/html;
    index index.html;

    ssl_certificate    /tls/service.pem;
    ssl_certificate_key /tls/service.key;

    location / {
        try_files $$uri /index.html;
    }

    location /auth {
        proxy_pass ${AUTH_URL:-http://127.0.0.1:8000};
    }

    location /callback/ {
        proxy_pass ${CALLBACK_URL:-http://127.0.0.1:8000};
        proxy_pass_request_headers      on;
        
    }

}